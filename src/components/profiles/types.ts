/**
 * Shared types for profile components
 */

/**
 * All available profile types in the application
 */
type ProfileType = "earlyCareer" | "midCareer" | "retirement" | "custom";

// Make sure we're explicitly exporting the type
export { ProfileType };